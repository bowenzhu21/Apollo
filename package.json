{
  "name": "spatial",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node -e \"console.log('Static build complete')\"",
    "check": "node --check src/main.js && node --check src/gestures.js && node --check src/api.js && node --check server.mjs && node --check api/chat.mjs && for file in lib/*.mjs; do node --check \"$file\"; done",
    "dev": "node server.mjs",
    "start": "node server.mjs",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
